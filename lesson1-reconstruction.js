/* Lesson 1 reconstruction — Part 1: First-Order ODEs.
   Authority: NTU ME2001-02 Sep. 7/9/14 sequence; mathematical source: supplied O'Neil 8th SI + instructor first-order notes.
   This file replaces Lesson 1 content in place so every later lesson keeps its existing unit indices/progress keys.
*/
(function(){
  if(!Array.isArray(window.units || (typeof units!=="undefined"?units:null))) return;
  const core='<span class="supplementalTag">Core · NTU Part 1 syllabus</span>';
  const book='<span class="supplementalTag">Authentic O’Neil · supplied 8th SI edition</span>';
  const notes='<span class="supplementalTag">Instructor first-order notes · Sep. 2026</span>';
  const explain='<span class="supplementalTag">Expanded explanation · beginner-first reconstruction</span>';
  const transfer='<span class="supplementalTag">Original transfer problem · course-aligned, not a textbook exercise</span>';
  const board=(title,prompt,solution,section='Part 1')=>({
    type:'bookproblem',bookSection:section,title,prompt,solution,
    practiceLabel:title.startsWith('O’Neil ')
      ? `Book practice · ${section}`
      : title.startsWith('Instructor ')
        ? 'Instructor-note practice · Sep. 2026'
        : 'Original course practice · not an O’Neil exercise'
  });

  const cooling=board('Instructor example · steel quench with Newton cooling',
    `<p>${notes}<br>A steel part begins at 850 °C and is quenched in oil held at 40 °C. After 20 s it reads 400 °C. Using Newton’s law <span class="math">T′=k(T−T_amb)</span>, determine <span class="math">k</span> and the time when the part reaches 100 °C.</p>`,
    `<p><b>Predict before calculating.</b> The object is hotter than the oil, so its temperature must fall toward 40 °C. Therefore <span class="math">k&lt;0</span>. A final answer that grows away from 40 °C has the wrong sign.</p>
     <p><b>1 · Solve the model.</b> Separate variables:</p><div class="eq">dT/(T−40)=k dt.</div><p>Integrate:</p><div class="eq">ln|T−40|=kt+C.</div><p>Exponentiating and using <span class="math">T(0)=850</span> gives</p><div class="eq">T(t)=40+810e^(kt).</div>
     <p><b>2 · Use the second measurement.</b></p><div class="eq">400=40+810e^(20k) ⇒ e^(20k)=360/810=4/9.</div><div class="eq">k=(1/20)ln(4/9)≈−0.04055 s⁻¹.</div><p>The unit s⁻¹ is required because <span class="math">kt</span> must be dimensionless.</p>
     <p><b>3 · Solve for 100 °C.</b></p><div class="eq">100=40+810e^(kt) ⇒ e^(kt)=2/27.</div><div class="eq">t=ln(2/27)/k≈64.2 s.</div>
     <div class="whybox"><b>Checks:</b> <span class="math">T(0)=850</span>; substituting <span class="math">t=20</span> gives 400; and because <span class="math">k&lt;0</span>, <span class="math">T(t)→40</span> as <span class="math">t→∞</span>, exactly as the physical story predicts.</div>`,'Instructor notes · §1.1 application');

  const mixing=board('Course application · mixing from rates and units',
    `<p>${notes}<br>A well-stirred tank contains 100 L of solution and initially 5 kg of salt. Brine at 0.1 kg/L enters at 2 L/min, and mixture leaves at 2 L/min. Let <span class="math">Q(t)</span> be salt mass in kg. Build and solve the IVP.</p>`,
    `<p><b>Story first:</b> volume stays 100 L because inflow equals outflow. Salt changes according to</p><div class="eq">rate of change = rate in − rate out.</div>
     <p><b>Rate in.</b></p><div class="eq">(0.1 kg/L)(2 L/min)=0.2 kg/min.</div>
     <p><b>Rate out.</b> The tank concentration is <span class="math">Q/100</span> kg/L, so</p><div class="eq">(Q/100)(2)=Q/50 kg/min.</div>
     <p>Hence</p><div class="eq">Q′+Q/50=0.2,  Q(0)=5.</div>
     <p><b>Predict the steady state before solving.</b> At equilibrium <span class="math">Q′=0</span>, so <span class="math">Q/50=0.2</span> and <span class="math">Q=10</span> kg. Since we start at 5 kg, the solution should rise toward 10.</p>
     <p><b>Solve the linear equation.</b> The integrating factor is <span class="math">I=e^(t/50)</span>:</p><div class="eq">(Qe^(t/50))′=0.2e^(t/50).</div><div class="eq">Qe^(t/50)=10e^(t/50)+C.</div><div class="eq">Q=10+Ce^(−t/50).</div><p>Use <span class="math">Q(0)=5</span>:</p><div class="eq">C=−5.</div><div class="whybox"><div class="eq">Q(t)=10−5e^(−t/50) kg.</div></div><p><b>Check:</b> it starts at 5, remains below 10, increases, and approaches the predicted 10 kg steady state.</p>`,'Instructor notes · linear application');

  const exactP2=board('O’Neil §1.3 Problem 2 · exact equation',
    `<p>${book}<br>Test and solve</p><div class="eq">(4xy+2x) + (2x²+3y²)y′ = 0.</div>`,
    `<p>Write differential form:</p><div class="eq">M dx+N dy=0,  M=4xy+2x,  N=2x²+3y².</div><p>Test exactness:</p><div class="eq">M_y=4x,  N_x=4x.</div><p>They agree, so a potential exists. Integrate <span class="math">M</span> with respect to <span class="math">x</span>, holding <span class="math">y</span> fixed:</p><div class="eq">φ=2x²y+x²+f(y).</div><p>Differentiate with respect to <span class="math">y</span> and match <span class="math">N</span>:</p><div class="eq">φ_y=2x²+f′(y)=2x²+3y² ⇒ f′(y)=3y².</div><div class="eq">f(y)=y³.</div><div class="whybox"><div class="eq">2x²y+x²+y³=C.</div></div><p>Differentiate this implicit relation to recover the original ODE; that is the verification.</p>`,'O’Neil §1.3 · Problem 2');

  const linearP3=board('O’Neil §1.2 Problem 3 · linear equation',
    `<p>${book}<br>Find the general solution of</p><div class="eq">y′+2y=x.</div>`,
    `<p>The equation is already in standard form <span class="math">y′+p(x)y=q(x)</span> with <span class="math">p=2</span>. Thus</p><div class="eq">I=e^(∫2dx)=e^(2x).</div><p>Multiply the <b>whole</b> equation:</p><div class="eq">e^(2x)y′+2e^(2x)y=xe^(2x).</div><p>The left side is the product derivative</p><div class="eq">(e^(2x)y)′=xe^(2x).</div><p>Integrate by parts:</p><div class="eq">∫xe^(2x)dx=(x/2)e^(2x)−(1/4)e^(2x).</div><p>Therefore</p><div class="eq">e^(2x)y=(x/2)e^(2x)−(1/4)e^(2x)+C.</div><div class="whybox"><div class="eq">y=x/2−1/4+Ce^(−2x).</div></div><p>Differentiate and substitute once to verify the result.</p>`,'O’Neil §1.2 · Problem 3');

  const desired=[
    {id:'l1-foundations-rebuilt',courseLesson:1,color:'#4059ad',badge:'1',label:'Foundations',title:'ODE Language Before ODE Methods',subtitle:'derivatives · integrals · forms · classification · IVPs',desc:'Build every prerequisite before a solving method asks you to use it.',lessons:[
      {title:'Read the notation',screens:[
        {type:'teach',title:'What an ODE is — and what the unknown actually is',html:`<p>${core}</p><p>A differential equation contains an unknown <b>function</b> and one or more of its derivatives. In this course the typical unknown is <span class="math">y=y(x)</span>. The independent variable is <span class="math">x</span>; <span class="math">y′=dy/dx</span> is the rate at which the function changes.</p><p>A first-order ODE contains <span class="math">y′</span> but no higher derivative. The <b>order</b> is the highest derivative present.</p>`},
        {type:'teach',title:'ODE versus PDE — and what “linear” actually means',html:`<p>${notes}</p><p>An <b>ordinary differential equation (ODE)</b> differentiates with respect to one independent variable. A <b>partial differential equation (PDE)</b> involves partial derivatives with respect to two or more independent variables. This semester focuses on ODEs.</p><p>An nth-order ODE is <b>linear in the dependent variable</b> when it can be written</p><div class="eq">a_n(x)y^(n)+a_(n-1)(x)y^(n-1)+⋯+a_1(x)y′+a_0(x)y=g(x).</div><p>The coefficients depend only on the independent variable. Thus <span class="math">y</span> and its derivatives appear only to the first power and are not multiplied together. Terms such as <span class="math">y²</span>, <span class="math">y y′</span>, <span class="math">sin y</span>, or <span class="math">(y′)³</span> make the equation nonlinear.</p><div class="tip"><b>Do not confuse order with linearity.</b> The equation <span class="math">y′′+5(y′)³−4y=e^x</span> is second order because <span class="math">y′′</span> is the highest derivative, but it is nonlinear because <span class="math">y′</span> is cubed.</div>`},
        {type:'teach',title:'Three forms you must be able to translate between',html:`<p>${notes}</p><div class="eq">y′=f(x,y)  (normal form)</div><div class="eq">F(x,y,y′)=0  (general form)</div><div class="eq">M(x,y)dx+N(x,y)dy=0  (differential form)</div><p>For the differential form, divide by <span class="math">dx</span> where appropriate: <span class="math">M+Ny′=0</span>, so if <span class="math">N≠0</span>, <span class="math">y′=−M/N</span>. Do not silently divide by an expression that might vanish; record the restriction.</p>`},
        {type:'quiz',title:'Order check',q:'What is the order of y′′+5(y′)³−4y=e^x?',options:['Second order','Third order','First order'],answer:0,why:'Order is determined by the highest derivative, not by the power on a derivative.'}
      ]},
      {title:'Calculus prerequisites before use',screens:[
        {type:'teach',title:'The derivative rules the course will use immediately',html:`<p>${explain}</p><p><b>Product rule:</b></p><div class="eq">(uv)′=u′v+uv′.</div><p><b>Chain rule:</b> if <span class="math">y=g(u(x))</span>, then</p><div class="eq">dy/dx=(dg/du)(du/dx).</div><p>These are not side facts. The product rule explains integrating factors; the chain rule explains substitutions such as <span class="math">y=ux</span> and <span class="math">v=y^(1−α)</span>.</p>`},
        {type:'teach',title:'Integration reverses a derivative — with one arbitrary constant',html:`<p>If <span class="math">y′(x)</span> is known,</p><div class="eq">y=∫y′(x)dx+C.</div><p>When both sides of a separated equation are integrated, you still need only <b>one</b> arbitrary constant: two integration constants can be combined into one.</p><p>Two antiderivatives that appear repeatedly are</p><div class="eq">∫du/u=ln|u|+C,   ∫du/(1+u²)=arctan(u)+C.</div><div class="warn"><b>Domain detail:</b> <span class="math">ln|u|</span> requires <span class="math">u≠0</span>. If you divided by <span class="math">u</span> to reach it, return later and test <span class="math">u=0</span> in the original equation.</div>`},
        {type:'quiz',title:'Why the absolute value?',q:'Why is ∫du/u written ln|u|+C?',options:['Because u may be positive or negative on an interval that does not cross 0','Because logarithms are always negative','Because u must equal 0'],answer:0,why:'d(ln|u|)/du=1/u for u≠0 on either a positive or negative interval.'}
      ]},
      {title:'Solutions and IVPs',screens:[
        {type:'teach',title:'General, particular, implicit — and verification',html:`<p>${core}</p><p>A <b>solution</b> is a function that satisfies the ODE on an interval. A family with an arbitrary constant is a <b>general solution</b>. Choosing the constant gives a <b>particular solution</b>.</p><p>An answer may be explicit, <span class="math">y=y(x)</span>, or implicit, <span class="math">Φ(x,y)=C</span>. An implicit relation can be a complete solution representation on an interval when its relevant branch or branches define differentiable functions that satisfy the ODE there (and satisfy any prescribed data). Implicit differentiation is the verification step; branch and domain restrictions still matter.</p><div class="whybox"><b>Verification rule:</b> never accept an answer because it “looks right.” Differentiate as needed and substitute it into the original equation.</div>`},
        {type:'teach',title:'What “singular solution” means in this course',html:`<p>${book}</p><p>A <b>singular solution</b> is a solution that is not represented by the ordinary one-parameter general family produced by the solving route. In separable equations this often appears when separation divides by a factor involving <span class="math">y</span>.</p><p>For example, solving <span class="math">y′=2xy²</span> by dividing by <span class="math">y²</span> assumes <span class="math">y≠0</span>. The resulting nonzero family does not recover <span class="math">y≡0</span> with a finite constant, yet direct substitution shows <span class="math">y≡0</span> is a valid solution. It must be reported separately.</p><div class="warn"><b>Terminology caution:</b> an equilibrium solution is a constant solution. In many separable examples an excluded equilibrium is also a singular solution, but the terms are not definitions of one another.</div>`},
        {type:'teach',title:'What an initial condition does geometrically',html:`<p>An IVP adds data such as</p><div class="eq">y(x₀)=y₀.</div><p>For a first-order family, this normally selects the integral curve passing through <span class="math">(x₀,y₀)</span>. Solve the general equation first, then fit the constant — unless an excluded/singular solution must be handled separately.</p>`}
      ]}
    ]},

    {id:'l1-separable-rebuilt',courseLesson:1,color:'#2e7d69',badge:'2',label:'Sep. 7',title:'Separable Equations & Lost Solutions',subtitle:'separate · integrate · restore excluded branches',desc:'The simplest method, taught with its most important trap attached.',lessons:[
      {title:'Method and logic',screens:[
        {type:'teach',title:'Recognition comes before algebra',html:`<p>${core}</p><p>A first-order equation is separable if it can be written</p><div class="eq">y′=F(x)G(y).</div><p>On a branch where <span class="math">G(y)≠0</span>, move the variables apart:</p><div class="eq">dy/G(y)=F(x)dx.</div><p>Then integrate both sides and solve explicitly if convenient. If not, an implicit relation is allowed.</p>`},
        {type:'teach',title:'The division step creates an obligation',html:`<p>${book}</p><p>If separating required division by <span class="math">G(y)</span>, you temporarily excluded every value satisfying <span class="math">G(y)=0</span>. After solving the nonzero branch, substitute each excluded constant value into the <b>original ODE</b>.</p><div class="warn">This is where singular/equilibrium solutions are commonly lost. Algebra may discard a perfectly valid solution.</div>`},
        {type:'quiz',title:'Lost-solution check',q:'When solving y′=2xy², dividing by y² excludes which candidate that must be checked separately?',options:['y=0','x=0','y=x'],answer:0,why:'The division assumes y≠0. In the original ODE, y≡0 gives y′=0 and the right side is also 0.'}
      ]},
      {title:'Authentic O’Neil example',screens:[
        {type:'teach',title:'O’Neil Example 1.1 — every restriction visible',html:`<p>${book}</p><p>For</p><div class="eq">y′=y²e^(−x),</div><p>first set aside <span class="math">y=0</span>. On a nonzero branch,</p><div class="eq">dy/y²=e^(−x)dx.</div><p>Integrate:</p><div class="eq">−1/y=−e^(−x)+C.</div><p>Rename the arbitrary constant:</p><div class="eq">y=1/(e^(−x)−k).</div><p>This family never gives <span class="math">y=0</span> for finite <span class="math">k</span>. But direct substitution shows <span class="math">y≡0</span> also solves the original ODE.</p><div class="whybox"><b>Complete answer:</b> the one-parameter nonzero family, on intervals where its denominator does not vanish, <b>plus</b> the singular solution <span class="math">y=0</span>.</div>`}
      ]}
    ]},

    {id:'l1-applications-rebuilt',courseLesson:1,color:'#8b6335',badge:'3',label:'Applications',title:'Model → Predict → Solve → Verify',subtitle:'cooling · decay · units · long-term behavior',desc:'Treat an application as a physical story before treating it as algebra.',lessons:[
      {title:'Cooling',screens:[
        {type:'teach',title:'Newton cooling — what the equation means before solving',html:`<p>${notes}</p><div class="eq">T′=k(T−T_amb).</div><p><span class="math">T−T_amb</span> is the temperature excess. If the body is hotter and cooling, <span class="math">T′&lt;0</span>, so <span class="math">k&lt;0</span>. The ambient temperature is an equilibrium because when <span class="math">T=T_amb</span>, the right side is zero.</p><p>This prediction is a built-in error check for the algebra.</p>`},
        cooling
      ]},
      {title:'Decay',screens:[
        {type:'teach',title:'Radioactive decay — proportional change produces an exponential',html:`<p>${notes}</p><div class="eq">m′=km,  k&lt;0.</div><p>Separate:</p><div class="eq">dm/m=kdt.</div><p>Integrate and exponentiate:</p><div class="eq">m=m₀e^(kt).</div><p>If <span class="math">h</span> is the half-life, then <span class="math">e^(kh)=1/2</span>, so</p><div class="eq">h=−ln2/k,   m(t)=m₀2^(−t/h).</div><p>The half-life does not depend on the starting amount, which is why it is a material property.</p>`}
      ]}
    ]},

    {id:'l1-homogeneous-rebuilt',courseLesson:1,color:'#7651b8',badge:'4',label:'Sep. 9',title:'Homogeneous First-Order Equations',subtitle:'recognize scaling · explain y=ux · reduce to separable',desc:'Introduce the substitution only after showing what structural problem it solves.',lessons:[
      {title:'Why y=ux works',screens:[
        {type:'teach',title:'Recognize the ratio structure first',html:`<p>${core}</p><p>If the slope can be written as</p><div class="eq">y′=g(y/x),</div><p>the equation depends on <span class="math">x,y</span> only through their ratio. Name that repeated ratio:</p><div class="eq">u=y/x  ⇒  y=ux.</div><p>This is the <b>purpose</b> of the substitution: it replaces every occurrence of <span class="math">y/x</span> by one new unknown <span class="math">u(x)</span>.</p>`},
        {type:'teach',title:'Differentiate the substitution with the product rule',html:`<p>${explain}</p><p>Because both <span class="math">u</span> and <span class="math">x</span> vary,</p><div class="eq">y=ux ⇒ y′=u+xu′.</div><p>Substitute into <span class="math">y′=g(y/x)</span>:</p><div class="eq">u+xu′=g(u).</div><div class="eq">du/(g(u)−u)=dx/x.</div><p>The result is separable. That is why the method works.</p><div class="warn">The division by <span class="math">x</span> means the argument is on an interval with <span class="math">x≠0</span>. Any other divided-out factor must also be checked.</div>`},
        {type:'quiz',title:'Differentiate correctly',q:'If y=u(x)x, what is y′?',options:['u+xu′','u′x only','u′+x'],answer:0,why:'Use the product rule because u depends on x.'}
      ]},
      {title:'Degree test and worked example',screens:[
        {type:'teach',title:'Differential-form recognition',html:`<p>${notes}</p><p>A function <span class="math">g(x,y)</span> is homogeneous of degree <span class="math">m</span> if</p><div class="eq">g(λx,λy)=λ^m g(x,y).</div><p>If <span class="math">M</span> and <span class="math">N</span> in <span class="math">Mdx+Ndy=0</span> have the same degree, their common scale cancels in <span class="math">y′=−M/N</span>, leaving a function of <span class="math">y/x</span>.</p>`},
        {type:'teach',title:'Instructor example — no algebra jump',html:`<p>${notes}</p><p>Solve</p><div class="eq">y′=(x²+y²)/(xy).</div><p>For <span class="math">x≠0</span>, divide numerator and denominator by <span class="math">x²</span>:</p><div class="eq">y′=(1+(y/x)²)/(y/x).</div><p>Set <span class="math">u=y/x</span> and <span class="math">y′=u+xu′</span>:</p><div class="eq">u+xu′=(1+u²)/u=u+1/u.</div><p>Subtract <span class="math">u</span>:</p><div class="eq">xu′=1/u.</div><p>Separate and integrate:</p><div class="eq">u du=dx/x ⇒ u²/2=ln|x|+C.</div><p>Restore <span class="math">u=y/x</span>:</p><div class="whybox"><div class="eq">y²/x²=2ln|x|+C.</div></div><p>The original denominator also requires <span class="math">xy≠0</span> wherever the normal form is used.</p>`}
      ]}
    ]},

    {id:'l1-linear-rebuilt',courseLesson:1,color:'#b35f4a',badge:'5',label:'Sep. 9',title:'Linear First-Order Equations',subtitle:'standard form first · derive the integrating factor',desc:'Place the syllabus linear lesson before exact equations and derive the method from the product rule.',lessons:[
      {title:'Derive the method',screens:[
        {type:'teach',title:'Standard form before anything else',html:`<p>${core}</p><div class="eq">y′+p(x)y=q(x).</div><p>The coefficient of <span class="math">y′</span> must be 1 before you identify <span class="math">p(x)</span>. If you have <span class="math">a(x)y′+b(x)y=r(x)</span>, divide by <span class="math">a(x)</span> only on intervals where <span class="math">a(x)≠0</span>.</p><div class="warn">Computing an integrating factor before standardizing is one of the most common errors.</div>`},
        {type:'teach',title:'Where I=e^(∫p dx) comes from',html:`<p>${explain}</p><p>We want multiplication by a function <span class="math">I(x)</span> to make the left side one product derivative:</p><div class="eq">I y′+I p y = (Iy)′=I y′+I′y.</div><p>For these to match, we need</p><div class="eq">I′=pI.</div><p>Separate:</p><div class="eq">I′/I=p ⇒ ln|I|=∫p dx.</div><p>Choose the convenient positive factor</p><div class="eq">I=e^(∫p dx).</div><p>Then</p><div class="eq">(Iy)′=Iq.</div><p>Integrate once and solve for <span class="math">y</span>. The integrating factor is therefore a consequence of the product rule, not a magic formula.</p>`},
        {type:'quiz',title:'Standard form trap',q:'For xy′+2y=x², what is p(x) after standardizing on x≠0?',options:['2/x','2','x²'],answer:0,why:'Divide the whole equation by x first: y′+(2/x)y=x.'}
      ]},
      {title:'Book and engineering practice',screens:[linearP3,mixing]}
    ]},

    {id:'l1-exact-rebuilt',courseLesson:1,color:'#3f7f8f',badge:'6',label:'Sep. 14',title:'Exact & Non-Exact Equations',subtitle:'partial derivatives only after the ODE foundation · reconstruct potential',desc:'Introduce partial derivatives exactly when the method needs them, then build the potential without skipped logic.',lessons:[
      {title:'Partial derivatives first',screens:[
        {type:'teach',title:'What M_y and N_x mean',html:`<p>${explain}</p><p>For a function of two variables, a partial derivative differentiates with respect to one variable while holding the other fixed.</p><p>Example:</p><div class="eq">M(x,y)=2xy+y³.</div><p>Holding <span class="math">x</span> fixed and differentiating with respect to <span class="math">y</span> gives</p><div class="eq">M_y=2x+3y².</div><p>This concept is introduced here — before asking you to use the exactness test.</p>`},
        {type:'teach',title:'Why exactness means a hidden potential exists',html:`<p>${notes}</p><p>For</p><div class="eq">Mdx+Ndy=0,</div><p>suppose there is a function <span class="math">φ(x,y)</span> with</p><div class="eq">φ_x=M,   φ_y=N.</div><p>Then <span class="math">Mdx+Ndy=dφ</span>, so the ODE says <span class="math">dφ=0</span> along a solution curve. Hence</p><div class="eq">φ(x,y)=C.</div><p>If the needed derivatives are continuous on a suitable region, equality of mixed partials gives the test</p><div class="eq">M_y=N_x.</div>`},
        {type:'quiz',title:'Test before reconstructing',q:'For Mdx+Ndy=0, what should you compute before integrating M or N to build φ?',options:['M_y and N_x','M_x and N_y only','A characteristic polynomial'],answer:0,why:'The exactness test prevents you from trying to reconstruct a potential that does not exist on the region.'}
      ]},
      {title:'Reconstruct the potential',screens:[
        {type:'teach',title:'Potential reconstruction — why the “constant” becomes f(y)',html:`<p>${explain}</p><p>If <span class="math">φ_x=M</span>, integrate with respect to <span class="math">x</span> while holding <span class="math">y</span> fixed:</p><div class="eq">φ=∫M dx+f(y).</div><p>Why <span class="math">f(y)</span>? Differentiating any function of <span class="math">y</span> with respect to <span class="math">x</span> gives zero, so the x-integration cannot detect it.</p><p>Next differentiate your candidate <span class="math">φ</span> with respect to <span class="math">y</span>, set it equal to <span class="math">N</span>, solve for <span class="math">f′(y)</span>, integrate, and finally write <span class="math">φ=C</span>.</p>`},
        exactP2
      ]},
      {title:'Non-exact: integrating-factor tests',screens:[
        {type:'teach',title:'Non-exact does not mean unsolvable',html:`<p>${notes}</p><p>If <span class="math">M_y≠N_x</span>, seek a nonzero factor <span class="math">μ</span> that makes <span class="math">μMdx+μNdy=0</span> exact. The instructor notes give these systematic tests:</p><div class="eq">(M_y−N_x)/N=f(x) ⇒ μ(x)=e^(∫f dx),</div><div class="eq">(M_y−N_x)/M=g(y) ⇒ μ(y)=e^(−∫g dy),</div><div class="eq">(M_y−N_x)/(N−M)=h(x+y) ⇒ μ=e^(∫h(z)dz), z=x+y.</div><p>For selected polynomial pairs, <span class="math">μ=x^αy^β</span> is a useful ansatz, not a guaranteed recipe.</p><div class="warn">Multiplying by <span class="math">μ</span> preserves solutions only on a region where the chosen factor is nonzero and defined. State that region.</div>`}
      ]}
    ]},

    {id:'l1-special-rebuilt',courseLesson:1,color:'#536b48',badge:'7',label:'Sep. 14',title:'Bernoulli, Riccati & Clairaut',subtitle:'recognize the pattern · explain the substitution · restore excluded solutions',desc:'Named forms are reductions to methods you already understand, not isolated tricks.',lessons:[
      {title:'Bernoulli',screens:[
        {type:'teach',title:'Bernoulli → linear, with the purpose stated first',html:`<p>${notes}</p><div class="eq">y′+p(x)y=q(x)y^α.</div><p>For <span class="math">α≠0,1</span>, the obstacle to linearity is the power of <span class="math">y</span>. We choose</p><div class="eq">v=y^(1−α)</div><p>because after dividing by <span class="math">y^α</span>, the chain rule makes <span class="math">y^(−α)y′</span> proportional to <span class="math">v′</span>. The transformed equation is</p><div class="eq">v′+(1−α)p(x)v=(1−α)q(x),</div><p>which is linear. If division by a power of <span class="math">y</span> excluded <span class="math">y=0</span>, test that solution separately.</p>`}
      ]},
      {title:'Riccati',screens:[
        {type:'teach',title:'Riccati → linear once one particular solution is known',html:`<p>${notes}</p><div class="eq">y′=P(x)y²+Q(x)y+R(x).</div><p>There is no universal elementary closed-form method. If one particular solution <span class="math">S(x)</span> is known, write</p><div class="eq">y=S+1/z.</div><p>The purpose is cancellation: substituting the fact that <span class="math">S</span> already satisfies the Riccati equation removes the quadratic obstruction and leaves a first-order linear equation in <span class="math">z</span>.</p><div class="warn">The substitution cannot produce <span class="math">y=S</span> for finite <span class="math">z</span>, so include the known particular solution separately if the transformed family does not contain it.</div>`}
      ]},
      {title:'Clairaut',screens:[
        {type:'teach',title:'Clairaut — line family plus possible singular envelope',html:`<p>${notes}</p><div class="tip"><b>Source/scope note:</b> Clairaut is explicitly included in the uploaded September first-order lecture notes even though it is not named separately in the short syllabus schedule. It is therefore retained as instructor-note material and is not attributed to O’Neil.</div><div class="eq">y=xy′+f(y′).</div><p>Set the constant slope <span class="math">y′=c</span> to get the family</p><div class="eq">y=cx+f(c).</div><p>Differentiate the original equation. With <span class="math">p=y′</span>,</p><div class="eq">p′(x+f′(p))=0.</div><p>The branch <span class="math">p′=0</span> gives the straight-line family. The other branch</p><div class="eq">x=−f′(p)</div><p>can produce a singular envelope that is not obtained by choosing any one constant <span class="math">c</span>.</p>`}
      ]}
    ]},

    {id:'l1-mastery-rebuilt',courseLesson:1,color:'#9a3f69',badge:'8',label:'Mastery',title:'Part 1 Method Selection & Transfer',subtitle:'curated source problems · no repetitive bank',desc:'Classify first, solve second, verify last. A small mixed set replaces long repetitive runs.',lessons:[
      {title:'Authentic source checks',screens:[
        board('O’Neil §1.1 Problem 12 · separable IVP',`<p>${book}<br>Solve</p><div class="eq">y′=3x²(y+2),  y(2)=8.</div>`,`<p>First note the equilibrium candidate <span class="math">y=−2</span>; it does not satisfy the given initial value. For the required branch:</p><div class="eq">dy/(y+2)=3x²dx.</div><div class="eq">ln|y+2|=x³+C.</div><p>Since <span class="math">y(2)+2=10&gt;0</span>, exponentiate on that positive branch:</p><div class="eq">y+2=Ae^(x³).</div><p>At <span class="math">x=2</span>, <span class="math">10=Ae^8</span>, so <span class="math">A=10e^(−8)</span>.</p><div class="whybox"><div class="eq">y=−2+10e^(x³−8).</div></div><p>Check <span class="math">y(2)=8</span> and differentiate to recover <span class="math">3x²(y+2)</span>.</p>`,'O’Neil §1.1 · Problem 12'),
        board('O’Neil §1.2 Problem 4 · linear equation',`<p>${book}<br>Solve</p><div class="eq">y′+sec(x)y=cos(x).</div>`,`<p>Work on an interval that does not cross a zero of <span class="math">cos x</span>, so <span class="math">sec x</span> is continuous.</p><p>The equation is already in linear standard form with <span class="math">p(x)=sec x</span>. Since</p><div class="eq">∫sec(x)dx=ln|sec(x)+tan(x)|,</div><p>we may choose the nonzero integrating factor</p><div class="eq">I(x)=sec(x)+tan(x)</div><p>on any such interval where this expression has fixed sign. Multiply the ODE:</p><div class="eq">(Iy)′=(sec(x)+tan(x))cos(x)=1+sin(x).</div><p>Integrate:</p><div class="eq">Iy=x-cos(x)+C.</div><div class="whybox"><div class="eq">y=[x-cos(x)+C]/[sec(x)+tan(x)].</div></div><p><b>Check:</b> differentiating <span class="math">Iy</span> gives <span class="math">1+sin x=I cos x</span>, which is exactly the multiplied equation.</p>`,'O’Neil §1.2 · Problem 4'),
        board('O’Neil §1.3 Problem 1 · exact equation',`<p>${book}<br>Test for exactness and, if exact, solve</p><div class="eq">2y²+y e^(xy)+(4xy+x e^(xy)+2y)y′=0.</div>`,`<p>Write <span class="math">M=2y²+y e^(xy)</span> and <span class="math">N=4xy+x e^(xy)+2y</span>. Then</p><div class="eq">M_y=4y+e^(xy)+xy e^(xy),</div><div class="eq">N_x=4y+e^(xy)+xy e^(xy).</div><p>The derivatives agree, so the equation is exact on the whole plane.</p><p>Integrate <span class="math">M</span> with respect to <span class="math">x</span>, holding <span class="math">y</span> fixed:</p><div class="eq">φ=2xy²+e^(xy)+f(y).</div><p>Differentiate with respect to <span class="math">y</span>:</p><div class="eq">φ_y=4xy+x e^(xy)+f′(y).</div><p>Match this to <span class="math">N</span>:</p><div class="eq">f′(y)=2y,   f(y)=y².</div><div class="whybox"><div class="eq">2xy²+e^(xy)+y²=C.</div></div><p>Differentiating this implicit relation reproduces the original differential equation.</p>`,'O’Neil §1.3 · Problem 1')
      ]},
      {title:'Mixed transfer',screens:[
        board('Transfer · classify before solving',`<p>${transfer}<br>For each equation, state the <b>first</b> method you would try and one reason: (a) <span class="math">y′=x(1+y²)</span>; (b) <span class="math">y′=(x+y)/x</span>; (c) <span class="math">xy′+2y=x²</span>; (d) <span class="math">(2xy+1)dx+(x²+2y)dy=0</span>; (e) <span class="math">y′+y=xy²</span>.</p>`,`<p><b>(a)</b> Separable: <span class="math">dy/(1+y²)=x dx</span>.<br><b>(b)</b> Homogeneous ratio: the right side is <span class="math">1+y/x</span>, so use <span class="math">u=y/x</span>.<br><b>(c)</b> Linear after dividing by <span class="math">x</span> on an interval with <span class="math">x≠0</span>.<br><b>(d)</b> Exact: <span class="math">M_y=2x=N_x</span>.<br><b>(e)</b> Bernoulli with exponent 2.</p><div class="whybox">The point is not to calculate yet. Correct classification prevents correct algebra with the wrong method.</div>`),
        board('Transfer · audit a flawed separation',`<p>${transfer}<br>A student solves <span class="math">y′=x(y−1)</span> by dividing by <span class="math">y−1</span>, obtaining <span class="math">ln|y−1|=x²/2+C</span>, and reports only the exponential family. What is missing?</p>`,`<p>The division assumed <span class="math">y−1≠0</span>, so the student must return to the original equation and test <span class="math">y=1</span>.</p><div class="eq">y≡1 ⇒ y′=0,  x(y−1)=0.</div><p>Thus <span class="math">y≡1</span> is a valid equilibrium solution. Depending on how the nonzero family is parameterized, it may or may not be represented by a finite constant; either way it must be accounted for explicitly during the audit.</p>`),
        board('Original transfer · special-equation decisions',`<p>${transfer}<br>This is original course practice because the goal is to mix the instructor’s Bernoulli, Riccati, and Clairaut extensions without importing later material. For each equation, identify the named form and state the transformation or branch split you would use before doing long algebra:<br>(a) <span class="math">y′+2y=xy³</span><br>(b) <span class="math">y′=y²+2xy+x²+1</span>, given that <span class="math">S(x)=-x</span> is one solution<br>(c) <span class="math">y=xy′-(y′)²</span>.</p>`,`<p><b>(a) Bernoulli.</b> Here the exponent is 3. Preserve any solution lost by division, then set <span class="math">v=y^(1-3)=y^(-2)</span>; the transformed equation is linear in <span class="math">v</span>.</p><p><b>(b) Riccati.</b> Use the supplied particular solution and set <span class="math">y=S+1/z=-x+1/z</span>. The point of the substitution is that the quadratic terms involving the known solution cancel, leaving a first-order linear equation for <span class="math">z</span>. Keep <span class="math">y=-x</span> separately if the transformed family misses it.</p><p><b>(c) Clairaut.</b> With <span class="math">p=y′</span>, the line family is <span class="math">y=cx-c²</span>. Differentiating gives <span class="math">p′(x-2p)=0</span>, so one must retain both the constant-slope branch and the possible singular-envelope branch <span class="math">x=2p</span>.</p><div class="whybox">The mastery target here is method selection with reasons, not pattern-name memorization.</div>`)
      ]},
      {title:'End-of-Part-1 gate',screens:[
        {type:'teach',title:'What “ready for Lesson 2” means',html:`<p>${core}</p><div class="whybox"><ol><li>Translate among normal, general, and differential forms without losing domain restrictions.</li><li>Classify before solving.</li><li>For separation, recheck every divided-out value.</li><li>For homogeneous equations, explain why <span class="math">y=ux</span> works.</li><li>For linear equations, standardize first and derive the integrating factor from the product rule.</li><li>For exact equations, know what a partial derivative and potential function mean before using the recipe.</li><li>For Bernoulli/Riccati/Clairaut, state the purpose of the substitution or branch split.</li><li>For applications, predict sign/steady state, track units, then verify the final solution.</li></ol></div><p>If any item is not reliable, return to that unit rather than memorizing another formula.</p>`}
      ]}
    ]}
  ];

  // Quality-reconstruction additions: fill source-backed gaps that were still too compressed
  // in the first reconstruction. These are inserted before fitting to the preserved Lesson-1 slots.
  const l1Unit=id=>desired.find(u=>u.id===id);

  const homogUnit=l1Unit('l1-homogeneous-rebuilt');
  if(homogUnit){
    homogUnit.lessons.push({title:'Generalized scaling ansatz',screens:[
      {type:'teach',title:'Instructor extension — y = u x^α is useful, but not guaranteed',html:`<p>${notes}</p><p>The equal-degree homogeneous test has a guarantee: if the equation reduces to a function of <span class="math">y/x</span>, then <span class="math">y=ux</span> makes it separable. The more general trial</p><div class="eq">y=u x^α</div><p>is different. Here <span class="math">α</span> is chosen to make powers of <span class="math">x</span> line up. It is an <b>ansatz</b>, not a theorem: a useful value of <span class="math">α</span> may not exist.</p>`},
      {type:'teach',title:'Instructor Example 1.2.5 — choose α by matching powers',html:`<p>${notes}</p><p>Solve</p><div class="eq">(y+x^4)dx-xdy=0.</div><p>Substitute <span class="math">y=ux^α</span>. Because <span class="math">u=u(x)</span>,</p><div class="eq">dy=x^α du+αx^(α-1)u dx.</div><p>Then</p><div class="eq">(ux^α+x^4)dx-x[x^α du+αx^(α-1)u dx]=0,</div><p>so</p><div class="eq">[(1-α)ux^α+x^4]dx-x^(α+1)du=0.</div><p>Divide by <span class="math">x^(α+1)</span> on an interval where <span class="math">x≠0</span>:</p><div class="eq">du=[(1-α)u/x+x^(3-α)]dx.</div><p>Choose <span class="math">α=1</span> because then the coefficient <span class="math">1-α</span> vanishes completely. The transformed equation collapses to</p><div class="eq">du=x^2 dx.</div><p>Integrate directly:</p><div class="eq">u=x^3/3+C.</div><p>Since <span class="math">y=ux</span>,</p><div class="whybox"><div class="eq">y=x^4/3+Cx.</div></div><p><b>Check:</b> differentiating gives <span class="math">y'=4x^3/3+C</span>; substituting into <span class="math">y+x^4-xy'=0</span> gives zero.</p><div class="tip"><b>Why this example is useful:</b> the original equation is not homogeneous in the equal-degree sense, so <span class="math">y=ux</span> was not guaranteed to help. Here it succeeds because the chosen scaling makes the unwanted <span class="math">u</span>-term cancel. A later linear-method viewpoint also solves the same equation, but that future method is not needed here.</div>`}
    ]});
  }

  const exactUnit=l1Unit('l1-exact-rebuilt');
  if(exactUnit){
    const nonexact=exactUnit.lessons.find(l=>l.title==='Non-exact: integrating-factor tests');
    if(nonexact) nonexact.screens.push(
      {type:'teach',title:'Instructor Example 1.4.1 — make a non-exact equation exact',html:`<p>${notes}</p><p>Solve</p><div class="eq">y dx+2x dy=0.</div><p>Here <span class="math">M=y</span> and <span class="math">N=2x</span>. Therefore</p><div class="eq">M_y=1,   N_x=2,</div><p>so the equation is not exact. Compute the x-only test:</p><div class="eq">(M_y-N_x)/N=-1/(2x).</div><p>On <span class="math">x&gt;0</span>,</p><div class="eq">μ(x)=e^(∫ -1/(2x) dx)=x^(-1/2).</div><p>Multiply the entire differential equation:</p><div class="eq">(y/√x)dx+2√x dy=0.</div><p>Now</p><div class="eq">∂(y/√x)/∂y=1/√x=∂(2√x)/∂x,</div><p>so it is exact. Reconstruct a potential by integrating the first coefficient with respect to <span class="math">x</span>:</p><div class="eq">φ=∫ yx^(-1/2) dx=2y√x+f(y).</div><p>Matching <span class="math">φ_y=2√x+f'(y)</span> to <span class="math">N^*=2√x</span> gives <span class="math">f'=0</span>. Hence</p><div class="whybox"><div class="eq">y√x=C.</div></div><p>The instructor notes deliberately revisit this example with a different integrating factor. That is not a contradiction: integrating factors need not be unique, and valid factors produce the same solution curves on their common domains.</p>`},
      {type:'quiz',title:'What does an integrating factor actually do?',q:'Why do we multiply a non-exact differential equation by μ?',options:['To make the new coefficients pass the exactness test','To increase the order of the ODE','To remove every domain restriction'],answer:0,why:'The goal is ∂(μM)/∂y = ∂(μN)/∂x, so a potential function can be reconstructed.'}
    );
  }

  const special=l1Unit('l1-special-rebuilt');
  if(special){
    const bern=special.lessons.find(l=>l.title==='Bernoulli');
    if(bern) bern.screens.push({type:'teach',title:'Instructor Example 1.6.1 — Bernoulli, fully worked',html:`<p>${notes}</p><p>Solve</p><div class="eq">y'-y=xy^2.</div><p><b>1 · Preserve the branch that division would lose.</b> Direct substitution shows <span class="math">y≡0</span> is a solution. For nonzero solutions, divide by <span class="math">y^2</span>:</p><div class="eq">y^(-2)y'-y^(-1)=x.</div><p><b>2 · Choose the Bernoulli variable.</b> Since <span class="math">α=2</span>, set</p><div class="eq">ν=y^(1-2)=y^(-1).</div><p>By the chain rule, <span class="math">ν'=-y^(-2)y'</span>. Multiply the divided equation by <span class="math">-1</span>:</p><div class="eq">ν'+ν=-x.</div><p><b>3 · Solve the resulting linear equation.</b> The integrating factor is <span class="math">e^x</span>:</p><div class="eq">(νe^x)'=-xe^x.</div><p>Integration by parts gives <span class="math">∫ -xe^x dx=-(x-1)e^x</span>, so</p><div class="eq">νe^x=-(x-1)e^x+C.</div><div class="eq">ν=1-x+Ce^(-x).</div><p><b>4 · Restore y.</b></p><div class="whybox"><div class="eq">y=1/(1-x+Ce^(-x)).</div><p>Together with the separate solution <span class="math">y≡0</span>.</p></div><p>Always state an interval on which the denominator is nonzero.</p>`});

    const ric=special.lessons.find(l=>l.title==='Riccati');
    if(ric) ric.screens.push({type:'teach',title:'Instructor Example 1.6.3 — Riccati cancellation, line by line',html:`<p>${notes}</p><p>Solve</p><div class="eq">y'=y^2-2xy+x^2+1.</div><p><b>1 · Find one particular solution.</b> Try <span class="math">S=x</span>. Then <span class="math">S'=1</span>, while the right side becomes <span class="math">x^2-2x^2+x^2+1=1</span>, so <span class="math">S=x</span> works.</p><p><b>2 · Substitute around the known solution.</b></p><div class="eq">y=x+1/z,   y'=1-z'/z^2.</div><p>On the right,</p><div class="eq">(x+1/z)^2-2x(x+1/z)+x^2+1=1+1/z^2.</div><p>Therefore</p><div class="eq">1-z'/z^2=1+1/z^2.</div><p>Subtract 1 and multiply by <span class="math">z^2</span>:</p><div class="eq">-z'=1.</div><p>Hence <span class="math">z=C-x</span>, so</p><div class="whybox"><div class="eq">y=x+1/(C-x).</div></div><p>The substitution <span class="math">1/z</span> cannot equal zero for finite <span class="math">z</span>, so it misses the known solution <span class="math">y=x</span>. The complete result is the one-parameter family above <b>plus</b> <span class="math">y=x</span>.</p>`});

    const clair=special.lessons.find(l=>l.title==='Clairaut');
    if(clair) clair.screens.push(
      {type:'teach',title:'Instructor Example 1.6.4 — find both Clairaut branches',html:`<p>${notes}</p><p>Solve</p><div class="eq">y=xy'+(y')^2.</div><p>This is Clairaut form with <span class="math">f(p)=p^2</span>, where <span class="math">p=y'</span>.</p><p><b>Line-family branch.</b> If <span class="math">p=c</span> is constant, then</p><div class="eq">y=cx+c^2.</div><p>Every value of <span class="math">c</span> gives a straight-line solution.</p><p><b>Why there can be another branch.</b> Differentiate the original equation:</p><div class="eq">y'=y'+xy''+2y'y''.</div><p>Subtract <span class="math">y'</span>:</p><div class="eq">0=y''(x+2y').</div><p>Thus a solution can satisfy either <span class="math">y''=0</span> (the line family) or</p><div class="eq">x+2y'=0.</div><p>On the second branch, <span class="math">y'=-x/2</span>. Substitute this value of <span class="math">y'</span> into the <b>original Clairaut equation</b>:</p><div class="eq">y=x(-x/2)+(-x/2)^2=-x^2/4.</div><div class="whybox"><b>Singular envelope:</b><div class="eq">y=-x^2/4.</div></div>`},
      {type:'teach',title:'Why the parabola is an envelope — and how to verify it',html:`<p>${explain}</p><p>Take a member of the line family <span class="math">y=cx+c^2</span>. Its slope is <span class="math">c</span>. The envelope condition from <span class="math">x=-f'(c)</span> gives <span class="math">x=-2c</span>, so <span class="math">c=-x/2</span>. At that contact point the line value is</p><div class="eq">cx+c^2=(-x/2)x+x^2/4=-x^2/4.</div><p>The line and parabola have the same slope there because the envelope derivative is <span class="math">(-x^2/4)'=-x/2=c</span>.</p><p><b>Direct ODE verification:</b> for <span class="math">y=-x^2/4</span>, <span class="math">y'=-x/2</span>, and</p><div class="eq">xy'+(y')^2=-x^2/2+x^2/4=-x^2/4=y.</div><p>So the envelope is genuinely a solution even though no single constant <span class="math">c</span> produces the whole parabola.</p>`},
      {type:'quiz',title:'Clairaut branch logic',q:'After differentiating y=xy′+f(y′), why must you keep both factors in y′′[x+f′(y′)]=0?',options:['They represent the line family and a possible singular envelope','One factor is always extraneous','Both factors mean y′ is constant'],answer:0,why:'Discarding either factor can discard an entire solution branch.'}
    );
  }

  const slots=[];
  for(let i=0;i<units.length;i++) if(Number(units[i]?.courseLesson||1)===1) slots.push(i);
  if(!slots.length) return;

  // Fit the reconstruction to the exact number of existing Lesson-1 slots. This preserves every
  // later lesson's absolute unit index and therefore does not scramble saved progress elsewhere.
  function fit(src,n){
    if(n===src.length) return src;
    if(n<src.length){
      const out=[];
      for(let i=0;i<n;i++){
        const a=Math.floor(i*src.length/n), b=Math.floor((i+1)*src.length/n);
        const group=src.slice(a,b);
        const first=group[0];
        out.push({...first,id:`l1-rebuilt-${i+1}`,badge:String(i+1),title:group.map(g=>g.title).join(' · '),subtitle:group.map(g=>g.subtitle).join(' · '),desc:group.map(g=>g.desc).join(' '),lessons:group.flatMap(g=>g.lessons)});
      }
      return out;
    }
    const out=src.map(x=>({...x}));
    const extras=[
      {id:'l1-method-clinic',courseLesson:1,color:'#6a6f7b',label:'Method clinic',title:'Method Selection Without Chapter Labels',subtitle:'overlap · fastest route · reasons before algebra',desc:'Mixed recognition practice where the method is not announced for you.',lessons:[{title:'Unsignaled classification',screens:[
        {type:'teach',title:'Some equations fit more than one category',html:`<p>${explain}</p><p>Classification is not always exclusive. An equation may be both separable and linear, or both homogeneous and exact. You do not earn extra credit for choosing the longest route. Choose a correct method whose assumptions are satisfied and whose algebra is clean.</p><div class="whybox"><b>Stable habit:</b> identify at least one valid structure, state its domain restrictions, then commit.</div>`},
        {type:'quiz',title:'Two valid routes',q:'For y′=xy, which descriptions are both true?',options:['Separable and linear homogeneous','Exact and Riccati only','Second-order and Bernoulli only'],answer:0,why:'It separates as dy/y=x dx and also has linear form y′−xy=0.'},
        {type:'quiz',title:'Do not force a named trick',q:'If an equation is already linear, should you prefer a more complicated substitution merely because one is possible?',options:['No — use the simplest valid route','Yes — named substitutions are always superior','Only if it removes the initial condition'],answer:0,why:'Method choice is about structure and reliability, not maximizing the number of transformations.'}
      ]}]},
      {id:'l1-verification-clinic',courseLesson:1,color:'#7b5a45',label:'Audit',title:'Verification, Domains & Source Honesty',subtitle:'substitute back · state intervals · never silently repair a source',desc:'A final audit discipline for answers and source-based study material.',lessons:[{title:'Audit habits',screens:[
        {type:'teach',title:'A boxed formula is not the end',html:`<p>${explain}</p><p>Before accepting a first-order solution, run three checks:</p><ol><li><b>Equation check:</b> differentiate and substitute into the original ODE.</li><li><b>Data check:</b> if it is an IVP, substitute the initial point.</li><li><b>Domain check:</b> state where denominators, logarithms, radicals, and substitutions are defined, and revisit values divided out.</li></ol><p>For an application, add a fourth check: units, sign, and long-term behavior must agree with the physical model.</p>`},
        {type:'teach',title:'How source discrepancies should be handled',html:`<p><span class="supplementalTag">Source-audit rule</span></p><p>If a supplied textbook or lecture source appears to contain a typo or inconsistency, do <b>not</b> silently rewrite it and then present the rewrite as the source. Preserve what the source actually says, flag the discrepancy, and show the independently verified correction separately.</p><p>This lesson does not invent a source error where none has been verified; it establishes the rule for any one we actually encounter.</p>`},
        {type:'quiz',title:'Domain audit',q:'You divided by y−2 while solving. What must you do before declaring the solution complete?',options:['Check y≡2 in the original equation','Assume y≠2 forever','Replace every y by |y|'],answer:0,why:'Division excluded that branch, so completeness requires testing it separately.'}
      ]}]}
    ];
    while(out.length<n){
      const i=out.length-src.length;
      const e=extras[i] || extras[extras.length-1];
      out.push({...e,badge:String(out.length+1)});
    }
    return out;
  }
  const fitted=fit(desired,slots.length);
  slots.forEach((idx,j)=>{units[idx]=fitted[j];});

  if(typeof courseLessons!=="undefined" && Array.isArray(courseLessons)){
    courseLessons[0]={number:1,title:'First-Order ODEs',subtitle:'Sep. 7–14 · terminology · separable · homogeneous · linear · exact/non-exact · Bernoulli/Riccati · instructor-note Clairaut',status:'complete'};
  }
  // Do not wipe global progress. The slot count is intentionally unchanged; later lessons keep their indices.
  if(typeof renderCourseMap==='function') renderCourseMap();
  if(typeof render==='function') render();
})();